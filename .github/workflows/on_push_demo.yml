name: Trigger wf on push
on: push
jobs:
  Print-if-Success:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v3
      - name: Print Status
        run: echo " Git checkout success"
